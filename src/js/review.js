const review = () => {
  const carousels = document.querySelector(".review_carousels");
  const cards = document.querySelectorAll(".review_carousels-card");
  console.log(cards);

  const btn_right = document.getElementById("arrow-right");
  const btn_left = document.getElementById("arrow-left");
  let current = 0;
};
review();

export default review;
